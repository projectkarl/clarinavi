const UA='Mozilla/5.0 (compatible; ClariNavi/Current; +https://vercel.app)';
const cache=new Map();
function num(v){if(v==null)return null;const n=Number(String(v).replace(/,/g,'').trim());return Number.isFinite(n)?n:null}
function roc(s){const x=String(s||'').replace(/\s/g,'');let m=x.match(/^(\d{2,3})\/(\d{1,2})\/(\d{1,2})$/);if(m)return `${+m[1]+1911}-${String(m[2]).padStart(2,'0')}-${String(m[3]).padStart(2,'0')}`;m=x.match(/^(\d{4})(\d{2})(\d{2})$/);return m?`${m[1]}-${m[2]}-${m[3]}`:x}
async function json(url,timeout=6000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{signal:c.signal,headers:{'User-Agent':UA,'Accept':'application/json,*/*'}});if(!r.ok)throw new Error(`HTTP ${r.status}`);return await r.json()}finally{clearTimeout(t)}}
function anchors(months){const out=[],d=new Date();d.setDate(1);for(let i=0;i<months;i++){const x=new Date(d.getFullYear(),d.getMonth()-i,1);out.push(`${x.getFullYear()}${String(x.getMonth()+1).padStart(2,'0')}01`)}return out.reverse()}
function parse(j){const out=[];const tables=Array.isArray(j?.tables)?j.tables:[];const candidates=[];if(Array.isArray(j?.data))candidates.push({fields:j.fields||[],data:j.data});for(const t of tables)if(Array.isArray(t?.data))candidates.push({fields:t.fields||[],data:t.data});for(const t of candidates){for(const r of t.data){if(!Array.isArray(r)||r.length<5)continue;const date=roc(r[0]),open=num(r[1]),high=num(r[2]),low=num(r[3]),close=num(r[4]);if(/^\d{4}-\d{2}-\d{2}$/.test(date)&&Number.isFinite(close))out.push({date,open,high,low,close})}}return out}
async function build(months){const key=String(months),hit=cache.get(key);if(hit&&Date.now()-hit.t<3600e3)return hit.v;let i=0;const aa=anchors(months),chunks=[];async function worker(){while(i<aa.length){const ix=i++,a=aa[ix];try{const j=await json(`https://www.twse.com.tw/indicesReport/MI_5MINS_HIST?date=${a}&response=json`,5500);chunks[ix]=parse(j)}catch{chunks[ix]=[]}}}await Promise.all(Array.from({length:Math.min(5,aa.length)},worker));const map=new Map();for(const r of chunks.flat())map.set(r.date,r);const rows=[...map.values()].sort((a,b)=>a.date.localeCompare(b.date));const v={symbol:'TAIEX',name:'加權指數',rows,source:'TWSE 發行量加權股價指數歷史資料'};cache.set(key,{t:Date.now(),v});return v}
module.exports=async function handler(req,res){res.setHeader('Cache-Control','s-maxage=3600, stale-while-revalidate=14400');res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Content-Type','application/json; charset=utf-8');const months=Math.max(1,Math.min(36,Number(req.query?.months)||12));try{return res.status(200).json(await build(months))}catch(e){return res.status(200).json({symbol:'TAIEX',rows:[],error:e.message,source:'TWSE'})}}
